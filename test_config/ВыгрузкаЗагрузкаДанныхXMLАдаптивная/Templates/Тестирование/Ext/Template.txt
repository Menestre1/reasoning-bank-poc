Тестирование

Все объекты выгружаемые выгружать в отдельные XML-файлы.
Все объекты после загрузки выгружать в отдельные XML-файлы в другую папку.
Если СравнитьФайлы() = Ок Тогда
	УдалитьОбаФайла();
КонецЕсли;

Смотреть, что останется.